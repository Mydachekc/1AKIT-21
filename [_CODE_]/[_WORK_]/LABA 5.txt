/* Main function of the C program. */

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int year, year2, year3;
    char sd[20], sd2[20], sd3[20];
    int io, io2, io3;
    int mgz, mgz2, mgz3;
    /*Данні*/
    printf ("1.Введіит: Наукового керівника, діаметр антени (м), робочу частоту (Мгц)");
    scanf  ("%d %s %d %d", &year, sd, &io, &mgz);
    printf ("2.Введіит: Наукового керівника, діаметр антени (м), робочу частоту (Мгц)");
    scanf  ("%d %s %d %d", &year2, sd2, &io2, &mgz2);
    printf ("3.Введіит: Наукового керівника, діаметр антени (м), робочу частоту (Мгц)");
    scanf  ("%d %s %d %d", &year3, sd3, &io3, &mgz3);
    /*таблиця*/
    printf ("|---------------------------------------------------------------------------|\n");
    printf ("|                       Пошук позаземних сигналів                           |\n");
    printf ("|   Рік    |    Науковий керівник   |   Діаметр антени  |   Робоча частота  |\n");
    printf ("|----------|------------------------|-------------------|-------------------|\n");
    /*Danni*/
    printf ("|%10d|%24s|%19d|%19d| \n", year, sd, io, mgz);
    printf ("|%10d|%24s|%19d|%19d| \n", year2, sd2, io2, mgz2);
    printf ("|%10d|%24s|%19d|%19d| \n", year3, sd3, io3, mgz3);
    /*All*/
    printf ("|---------------------------------------------------------------------------|\n");
    printf ("|Примітка: спостерігались об'єкти від 2 зірок до декількох галактик         |\n");
    printf ("|---------------------------------------------------------------------------|\n");
}



/* Main function of the C program. */

#include <stdio.h>
#include <stdlib.h>

int main()
{
  int a, b, c;
  
  printf ("Введіть А і В:");
  scanf ("%d %d", &a, &b);
  
    if (a > 20 && a >= b)
        printf ("Відповідь: А більше число воно = %d", a);
    else if (b > a)
        printf ("Відповідь: B більше число воно = %d", b);
    else
        printf ("Відповідь: false ");
 
}
